# PDEvo

This is the anonymous implementation of **PDEvo**.

The repository contains the core model, training entry, and necessary utility files. The main model is implemented in `models/PDEvoNet.py`.

## Structure

```text
PDEvo/
├── data_provider/
├── exp/
├── layers/
├── models/
│   └── PDEvoNet.py
├── utils/
├── README.md
├── requirements.txt
└── run.py
